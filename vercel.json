{
  "version": 2,
  "buildCommand": "echo skip",
  "builds": [
    { "src": "api/server.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/server" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
